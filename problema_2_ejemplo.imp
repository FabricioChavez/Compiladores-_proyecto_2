var int a, b;

fun int suma(int x)
  var int accum;
  accum = 0;
  while 0 < x do
    var int zz;
    accum = accum + x;
    x = x - 1
  endwhile;
  print(accum);
  return (accum)
endfun

fun void imprimir(int x , int y )
    var int h;
    h = 1;
    print(x+h);
    print (y);
    return ()
endfun


fun void main()
  var int y;
  a = 10;
  y = 11;
  imprimir ( y  , y+10);
  suma(a);

  return ()
endfun
